# Python 3.8 / legacy Anaconda compatibility
# NumPy 1.24 removed legacy aliases such as np.int, which some older
# visualization stacks still import. Keep NumPy 1.23.x on Python 3.8.
numpy>=1.23,<1.24; python_version < "3.9"
matplotlib>=3.5,<3.8; python_version < "3.9"

# Newer Python environments can use current compatible releases.
numpy>=1.23; python_version >= "3.9"
matplotlib>=3.5; python_version >= "3.9"

Pillow>=8.0
